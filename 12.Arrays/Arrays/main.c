#include <stdio.h>
#include <stdlib.h>

int main()
{
    char luckyLetters[] = {'A', 't', 'b', 'e', 'Y'};
    printf("%c", luckyLetters[1]);//This is for the position of the letter in the array

    return 0;
}
